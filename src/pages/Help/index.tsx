import React, { FC } from 'react';
import Template from '../../components/Template';
import './styles.css';

const Help: FC = () => (
  <div>
    <Template isStudent>
      <h1 className="title">Ajuda</h1>
    </Template>
  </div>
);

export default Help;
